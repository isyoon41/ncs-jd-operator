# 인수인계서 — 2026-08-11

## 프로젝트 개요

- **경로**: `C:\Users\LG\OneDrive\01_PROJECTS\업무자동화_개발\NCS 기반 직무기술서 생성기`
- **저장소**: `https://github.com/isyoon41/ncs-jd-operator` (Public)
- **브랜치**: `master`
- **스택**: Next.js 16.2.12 App Router/Turbopack, React 19.2.4, TypeScript, Tailwind CSS v4, Supabase, Google Gemini API, `@react-pdf/renderer`, Vitest, lucide-react
- **Supabase 프로젝트**: `ncs-jd-generator`, project ref `yobjuffwfklzpmeriiwz`, Seoul 리전
- **운영 URL**: `https://pathwaypartners-ncsjd-operator.vercel.app` (구 URL `https://ncs-jd-operator.vercel.app`도 같은 배포를 가리킴)
- **Vercel**: `isyoon41s-projects` 범위, GitHub `master` 연동 자동 배포
- **최신 기능 커밋**: `4005357 Allow repeated JD refinements` (`master`, `origin/master` 일치). 이 인계서 커밋은 `/인계` 종료 단계에서 뒤이어 생성한다.

## 오늘 작업 내용

1. **Evidence coverage를 모델 점수에서 코드 계산으로 전환** (`39c7d2b`)
   - Gemini가 근거 없이 92~95점대에 수렴하던 문제를 확인했다.
   - `src/lib/jd/coverage-score.ts`에서 `ai_inference` 비율, finding severity, NCS 연결 강도 등 검증된 데이터로 직접 계산하도록 바꿨다.

2. **관리자 콘솔과 조직 전환 성능·안정성 개선** (`4a6bde5`, `1c63ed8`, `6e93e40`, `6aa7a22`)
   - 회사별 사용자 테이블, 초대 링크 복사, 역할 변경 UI를 구현했다.
   - `src/lib/auth/session.ts`의 React `cache()` 기반 `getSessionUser`/`getOrgContext`로 요청당 인증 왕복을 줄였다.
   - Supabase Admin API 회원 목록에 페이지네이션을 적용했다.
   - 오래된 회사 프로필 누락 필드를 `normalizeCompanyContext`로 보정했다.
   - 관리자 RPC의 `auth.users.email varchar → text` 타입 불일치를 migration으로 수정했다.
   - 플랫폼 운영자 `isyoon@wisdomnco.com`은 특정 회사 소속 행 없이도 전사 접근하도록 정리하고, 관리자 화면에서도 회사 소속과 분리해 표시했다.

3. **관리자/일반 사용자 권한을 실제로 분리** (`d76ec0f`)
   - `member`는 조회 전용, `admin`/플랫폼 운영자는 회사 프로필·팀·JD 관련 쓰기가 가능하도록 RLS와 서버 액션을 함께 수정했다.
   - 일반 사용자에게는 쓰기 버튼을 감추고 읽기 전용 화면·안내를 표시한다.
   - 사용자가 운영 환경에서 관리자와 일반 사용자 권한 분리가 의도대로 작동함을 직접 확인했다.

4. **Supabase 로컬/원격 마이그레이션 이력 정합화** (`af590b9`)
   - 인위적 `000...` 로컬 버전을 실제 원격 적용 시각 `202608...` 버전으로 정리했다.
   - 운영 객체를 읽기 전용으로 확인한 뒤 누락 이력 `20260804093011`, `20260804093012`만 `applied` repair했다. SQL 본문은 재실행하지 않았다.
   - 최종 로컬/원격 13개가 1:1 일치하고 `supabase db push --linked --dry-run`은 up-to-date다.
   - 상세 대응표와 검증 근거: `docs/MIGRATION_HISTORY_20260811.md`.

5. **JD 정확도 개선 지시 1~4번 완료** (`8110c5f`, `d53bebd`, `a986ec5`, `f206782`)
   - v1.1 보완 시 내용이 동일한 책임·필수/우대 자격요건은 직전 `basis`/`ncsCodes`를 content 문자열 기준으로 그대로 유지한다. 순서 변경도 안전하다.
   - NCS 후보 검색은 능력단위명뿐 아니라 `definition`도 검색하고, 대분류 일치는 하드 필터가 아닌 가중치로 반영해 관련도 상위 50개를 선택한다.
   - Gemini가 허용 후보 밖의 NCS 코드를 제안해 제외될 때 조용히 버리지 않고 validation `info` finding에 코드 목록과 건수를 표시한다. v1.0 생성 단계에서 버린 코드도 최종 검증으로 전달한다.
   - v1.x 편집 화면에서 `basis === "ai_inference"`인 책임·자격요건을 `확인 필요` 배지와 안내 목록으로 표시한다.

6. **공식 NCS 능력단위 정의·분류 데이터 보강** (`04f44d1`)
   - 공식 공공데이터 CSV와 NCS 포털 상세조회로 기존 `ncs_competency_units` 행의 정의·수준·분류를 보강하는 운영 스크립트를 만들었다.
   - 운영 DB 13,442행 수는 유지됐고, 정의 13,398행(99.67%), 네 단계 분류명 13,442행(100%)을 확보했다.
   - 정의가 없는 44개는 모두 19번 대분류의 과거 전자기기 개발 코드다. 정확한 같은 버전의 공식 정의가 없어 다른 버전 정의를 임의 복사하지 않았다.
   - 실행·검증 절차: `docs/NCS_DATA_SYNC.md`. 스크립트는 기본 dry-run이며 `--apply`를 명시해야 운영 반영한다.

7. **JD 업데이트 횟수 제한 제거** (`4005357`)
   - 상세 화면, 편집 페이지, 서버 액션에 있던 `v1.1에서 종료` 조건을 제거했다.
   - `src/lib/jd/versioning.ts`에서 현재 버전과 다음 버전 라벨을 계산하므로 v1.0 → v1.1 → v1.2 → v1.3처럼 계속 갱신할 수 있다.
   - 각 저장은 새 `jd_versions` 행을 만들고 직전 버전을 보존한다. 앞서 구현한 content 기반 근거 승계도 매 반복마다 최신 스냅샷을 기준으로 작동한다.
   - 상세·편집·홈·회사 프로필·신규 생성 화면의 고정 v1.1 문구를 현재/다음 버전에 맞는 동적 문구로 바꿨다.
   - 로그인된 퓨리오젠 계정의 실제 v1.1 문서에서 `v1.2 업데이트` 버튼과 `직무기술서 v1.2 만들기`, 기존 v1.1 보존 안내를 브라우저로 확인했다. 의도치 않은 DB 변경을 피하려고 마지막 저장 버튼은 누르지 않았다.

## 현재 상태

- **Git**: `/인계` 시작 시 `master`와 `origin/master`가 `4005357`에서 일치했고 작업 트리는 clean이었다. 이 문서만 새 변경으로 추가한 뒤 별도 인계 커밋·푸시한다.
- **최근 기능 커밋** (최신순):
  - `4005357` Allow repeated JD refinements
  - `04f44d1` Enrich NCS definitions from official sources
  - `f206782` Flag AI-inferred items during JD refinement
  - `a986ec5` Report discarded NCS codes in validation
  - `d53bebd` Improve NCS candidate retrieval relevance
  - `8110c5f` Preserve unchanged JD evidence on refinement
  - `af590b9` Reconcile Supabase migration history
  - `83f24de` Add handoff for coverage scoring, admin console rebuild, and RLS permission split
- **검증**: `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test` 통과. Vitest 43개와 NCS 동기화 Node 테스트 4개 통과.
- **실화면 검증**: 퓨리오젠 v1.1 상세에서 v1.2 업데이트 진입까지 확인. 실제 v1.2 저장은 아직 하지 않았다.
- **배포**: GitHub `master` 푸시 후 Vercel 자동 배포 방식. 운영 배포 완료 여부는 Vercel 대시보드 또는 운영 URL에서 확인한다.
- **Supabase**: 로컬/원격 migration 13개 일치, 미적용 migration 0개. 이번 JD 정확도·반복 업데이트 작업에는 스키마 변경이 없다.
- **로컬 서버**: 마지막 확인 시 `http://localhost:3000`에서 Next.js 개발 서버가 실행 중이었다. 다음 세션에서 포트 상태를 다시 확인한다.

## 아키텍처 핵심 패턴

- **제품 원칙**: 회사 소개/IR 자료와 최소 팀 입력으로 Gemini가 NCS 근거형 JD v1.0을 생성한다. 사용자는 선택적으로 보완하며 v1.x를 필요한 만큼 반복 생성하고, 매번 NCS 검증 루프를 다시 실행한다.
- **버전은 누적 저장**: `jd_versions.version_no`는 내부 순서, `version_major`/`version_minor`는 표시용 의미 버전이다. 반복 보완은 기존 행을 덮어쓰지 않고 다음 minor 버전의 새 행을 만든다. 버전 계산은 `src/lib/jd/versioning.ts`를 사용한다.
- **근거 보존은 content 기준**: `preserveUnchangedGrounding`은 직전 `design_snapshot`의 책임·자격요건을 내용 문자열로 매칭한다. index 기준으로 바꾸지 않는다. 수정/신규 문장만 `team_input`, 빈 NCS 코드로 시작한다.
- **NCS 검색과 선택을 분리**: 검색은 name/definition 매칭과 분류 가중치로 후보를 만들고, Gemini는 허용 후보 안에서만 매핑한다. 허용 밖 코드는 제거하되 반드시 validation finding으로 사용자에게 알린다.
- **AI 추론은 숨기지 않는다**: `basis`는 `company | team_input | ncs | ai_inference` 중 하나다. `ai_inference` 항목은 편집 화면에서 확인 대상으로 보여야 한다.
- **공식 NCS 동기화는 보수적으로**: 기존 코드만 update하고 행을 임의 추가하지 않는다. 같은 명칭의 다른 버전 정의를 복사하지 않는다. 일부 공식 조회 실패 시 기본적으로 DB 반영 전에 중단한다.
- **인증/조직 컨텍스트는 `src/lib/auth/session.ts` 경유**: 페이지에서 `supabase.auth.getUser()`와 조직 조회를 중복 호출하지 않는다.
- **권한**: 조회는 `is_org_member`, 쓰기는 `is_org_admin`. UI의 `memberRole`로 쓰기 UI를 숨기되 서버 액션 권한 검사와 RLS를 최종 방어선으로 유지한다.
- **플랫폼 운영자는 회사 소속 행이 없다**: 플랫폼 관리자 권한으로 모든 회사에 접근하며 `organization_members`에 행을 만들지 않는다.
- **DB 변경 절차**: Supabase MCP `apply_migration`으로 운영에 적용하고 동일 SQL을 `supabase/migrations/`에 커밋한다. RLS는 impersonation으로 실제 거부/허용을 검증한다.
- **페이지 레이아웃**: 로그인 후 페이지는 `PageContainer`/`PageHeader`를 재사용한다. Service Role 코드는 서버 전용 모듈에만 둔다.
- **검증 후 배포**: `npx tsc --noEmit`, `npm run lint`, `npm run build`, `npm test`를 모두 통과한 변경만 `master`에 푸시한다.

## 주요 파일 경로

- `src/lib/jd/company-designer.ts` — Gemini 생성/검증, NCS 후보 조회, design 정규화
- `src/lib/jd/refinement.ts` / `refinement.test.ts` — 변경 없는 문장의 근거 승계
- `src/lib/jd/ncs-retrieval.ts` / `ncs-retrieval.test.ts` — 후보 관련도 점수·정렬
- `src/lib/jd/ncs-code-validation.ts` / `ncs-code-validation.test.ts` — 제외된 무효 NCS 코드 finding
- `src/lib/jd/versioning.ts` / `versioning.test.ts` — 반복 v1.x 버전 계산과 회귀 테스트
- `src/lib/jd/coverage-score.ts` — Evidence coverage 코드 계산
- `src/app/(app)/jobs/new/actions.ts` — 회사/팀 최소 입력 기반 v1.0 생성
- `src/app/(app)/jobs/[id]/edit/actions.ts` — 최신 버전 기반 반복 보완·NCS 재검증·새 버전 저장
- `src/app/(app)/jobs/[id]/page.tsx` — 최신 JD 상세와 다음 업데이트 진입
- `src/app/(app)/jobs/[id]/edit/page.tsx` — 최신 버전을 입력값으로 쓰는 동적 v1.x 편집 화면
- `src/components/jobs/jd-refine-form.tsx` — 선택 보완 폼, AI 추론 확인 안내, 동적 버전 문구
- `scripts/sync-ncs-details.mjs`, `scripts/ncs-sync-lib.mjs` — 공식 NCS 정의/분류 동기화
- `docs/NCS_DATA_SYNC.md` — NCS 데이터 운영 절차와 현재 커버리지
- `src/lib/auth/session.ts` — 캐시된 사용자/조직 컨텍스트
- `src/components/admin/company-members-panel.tsx` — 회사별 회원 관리
- `supabase/migrations/20260811045817_admin_write_member_readonly.sql` — 관리자 쓰기/일반 읽기 권한 분리
- `docs/MIGRATION_HISTORY_20260811.md` — 마이그레이션 정합화 근거와 대응표

## 환경변수

실제 값은 `.env.local` 및 Vercel 환경변수에만 두고 Git/인계서에는 기록하지 않는다.

- `NCS_API_SERVICE_KEY`
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`
- `GEMINI_API_KEY`
- `SENTRY_DSN`, `NEXT_PUBLIC_SENTRY_DSN` — 선택
- `SENTRY_ORG`, `SENTRY_PROJECT`, `SENTRY_AUTH_TOKEN` — 선택

빈 값 템플릿은 `.env.local.example`을 참고한다.

## 미해결 과제

1. **실제 v1.2 저장과 다음 반복 검증 (최우선)**
   - 퓨리오젠 v1.1 편집 화면에서 의도한 작은 수정 후 저장해 v1.2가 생성되는지 확인한다.
   - 저장 후 상세 화면에 `v1.3 업데이트`가 나타나는지 확인한다.
   - DB의 v1.2 `jd_sections`/`jd_evidence`/`role_ncs_mappings`를 확인해 수정하지 않은 책임 문장의 NCS 근거가 v1.1과 동일하게 남았는지 검증한다.

2. **버전 이력 UI**
   - DB에는 모든 버전이 누적되지만 화면은 최신 버전만 보여준다. 사용자가 v1.0/v1.1/v1.2를 비교·열람·복원하는 UI는 아직 없다.

3. **NCS 정의 미확보 44개 추적**
   - 과거 전자기기 개발 코드의 동일 버전 공식 정의가 새 CSV/포털에 제공되면 `sync:ncs-details -- --download-source --portal --refresh-portal`로 재검토한다.
   - 다른 버전 정의를 임의 복사하지 않는다.

4. **정확도 개선 후 실사용 표본 평가**
   - 서로 다른 산업의 회사 소개 2~3개로 v1.0/v1.x를 만들어 name-only 검색 대비 definition 검색이 실제 후보와 최종 JD를 개선했는지 기록한다.
   - Evidence coverage가 과거 92~95 좁은 범위에서 벗어나 실제 근거 품질에 따라 구분되는지 확인한다.

5. **후순위 제품 과제**
   - 문장별 원문·페이지 근거 추적, `ncs_qualifications` 연결, 다차원 coverage, 책임 구조 확장, NCS 임베딩/pgvector는 아직 착수하지 않았다. 모두 스키마·파이프라인 규모가 커 별도 설계 후 진행한다.
   - 가입 요청 UX, PDF 자동 테스트, Sentry 활성화, npm audit 취약점 검토도 남아 있다.

## 주의사항

- `.env.local`, Supabase/Gemini/NCS/Sentry 키, DB 비밀번호, Vercel 토큰, 브라우저 로그인 정보는 절대 커밋하거나 문서에 기록하지 않는다.
- **JD 반복 보완에서 직전 스냅샷 대신 v1.0을 고정 참조하지 않는다.** 항상 최신 `jd_versions`를 기준으로 다음 버전과 근거를 만든다.
- **`preserveUnchangedGrounding`을 index 매칭으로 바꾸지 않는다.** textarea 줄 순서가 바뀔 수 있으므로 content 문자열 매칭이 회귀 버그 방지의 핵심이다.
- **Gemini가 허용 후보 밖 코드를 제안해도 조용히 숨기지 않는다.** 제외는 안전상 필요하지만 finding으로 사용자에게 알려야 한다.
- AI 생성 횟수 보호 한도(현재 사용자 시간당 5건, 회사 하루 30건)는 버전 수 제한과 별개다. 반복 버전은 무제한이지만 짧은 시간 내 호출은 rate limit을 적용한다.
- 회사 프로필/JD/팀 RLS의 insert/update/delete에는 `is_org_admin`, select에는 `is_org_member`를 사용한다.
- 플랫폼 운영자에게 `organization_members` 행을 만들지 않는다.
- 마이그레이션 파일명은 `supabase migration new`가 생성한 실제 타임스탬프를 유지하고 다시 `000...` 순번으로 바꾸지 않는다.
- NCS 동기화 `--apply`는 운영 DB 쓰기다. dry-run 수치와 실패 목록을 먼저 확인하고 일부 실패를 `--allow-partial`로 무시하지 않는다.
- Vercel CLI는 로그아웃 상태일 수 있다. 배포 토큰을 셸 명령이나 코드에 직접 넣지 않는다.
- 공식 로고는 저장소 자산을 사용하고 CSS/텍스트로 워드마크를 다시 만들지 않는다.
- `deleteUserAccount`, `deleteRole`은 되돌릴 수 없는 작업이다.
- 작업 트리에 다른 사용자의 변경이 있으면 덮어쓰지 말고 diff로 범위를 확인한다.
