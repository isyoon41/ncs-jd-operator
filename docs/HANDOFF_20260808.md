# 인수인계서 — 2026-08-08

## 프로젝트 개요

- **경로**: `C:\Users\LG\OneDrive\01_PROJECTS\업무자동화_개발\NCS 기반 직무기술서 생성기`
- **저장소**: `https://github.com/isyoon41/ncs-jd-operator` (Public)
- **브랜치**: `master`
- **스택**: Next.js 16.2.12 App Router/Turbopack, React 19.2.4, TypeScript, Tailwind CSS v4, Supabase, Google Gemini API, `@react-pdf/renderer`, Vitest, lucide-react
- **Supabase 프로젝트**: `ncs-jd-generator`, project ref `yobjuffwfklzpmeriiwz`, Seoul 리전
- **현재 운영 URL**: `https://ncs-jd-operator.vercel.app`
- **변경 희망 URL**: `https://pathwaypartners-ncsjd-operator.vercel.app` — 아직 미적용
- **Vercel**: `isyoon41s-projects` 범위, Hobby 플랜, GitHub 연동 자동 배포
- **최신 구현 커밋**: `beb088f Use official transparent Pathway branding` (`master`, `origin/master` 일치)

## 오늘 작업 내용

1. **공유 인증 레이아웃과 조직 전환**
   - 인증이 필요한 페이지를 `src/app/(app)/` 라우트 그룹으로 정리했다.
   - 공통 헤더에 로그인 이메일, 로그아웃, Super Admin 링크, 회사 전환 기능을 추가했다.
   - 활성 회사는 `active_org_id` httpOnly 쿠키로 유지하며 `src/lib/org/active-organization.ts`의 공용 로직으로 해석한다.

2. **회사 프로필 편집과 JD 반영**
   - `/company`에서 AI가 추출한 회사 미션·비전·핵심가치·사업영역을 관리자가 직접 수정할 수 있다.
   - 수정은 기존 행을 덮지 않고 새 버전으로 추가한다.
   - v1.1 보완 시 조직의 최신 회사 프로필을 다시 조회하도록 수정해 편집 결과가 실제 JD에 반영된다.

3. **가입 승인 및 관리자 회원 관리**
   - 가입자는 회사명을 입력해 `organization_access_requests`에 접근 요청을 만들고 플랫폼 관리자가 승인/거절한다.
   - 전체 회원 조회, 보류/활성화, 비밀번호 초기화, 계정 삭제, 소속 회사 변경, 승인과 동시에 새 회사 생성 기능을 구현했다.
   - 관련 마이그레이션은 `00000000000007`부터 `00000000000010`까지다.
   - 주요 구현 커밋: `482cf16`, `898371f`, `8197f26`, `27bc66a`, `a24a632`, `8f67b0a`; 이전 인계 커밋은 `9c812e0`이다.

4. **Pathway Partners 공식 브랜딩 1차 적용** (`9b248c2`)
   - 로그인/회원가입 화면 왼쪽에 Pathway Partners 브랜드 패널을 추가했다.
   - 인증 후 헤더에 `A PATHWAY PARTNERS PRODUCT` 표기를, 앱 공통 레이아웃에 브랜드 푸터를 추가했다.
   - 페이지 제목을 `NCS JD Operator | Pathway Partners`로 변경했다.

5. **공식 CI 원본 기반 로고 및 투명 파비콘 보정** (`beb088f`)
   - 공식 원본 `G:\공유 드라이브\위즈덤앤코\00_법인서류&그룹HR\01 위즈덤&패스웨이 법인서류\CI 및 명함 디자인\2026 패스웨이파트너스 CI\logo 사본.pdf`에서 워드마크와 심볼을 벡터 렌더링해 추출했다.
   - HTML 텍스트로 조합한 유사 워드마크를 제거하고 공식 전체 워드마크 PNG를 사용한다.
   - 어두운 인증 패널 상단에는 흰색 전체 워드마크, 하단에는 낮은 불투명도의 전체 로고와 저작권 문구가 표시된다.
   - `src/app/icon.png`은 256×256, `src/app/apple-icon.png`은 512×512 투명 PNG다. 로컬 및 공개 배포 파일 모두 알파 채널과 모서리 `alpha=0`을 확인했다.
   - 데스크톱 로그인 화면, 타입 검사, ESLint, 프로덕션 빌드를 통과했다.

6. **Vercel 프로젝트/도메인 이름 변경 시도**
   - 사용자가 `ncs-jd-operator`를 `pathwaypartners-ncsjd-operator`로 바꾸도록 요청했다.
   - 목표 URL `https://pathwaypartners-ncsjd-operator.vercel.app`은 현재 Vercel 404를 반환해 사용 중인 배포는 없는 것으로 보인다.
   - 로컬 Vercel CLI 50.32.5에는 로그인 자격증명이 없고, 연결된 브라우저도 Vercel 로그인 화면으로 이동했다. Chrome/Edge 연결은 사용할 수 없었다.
   - Vercel 로그인 탭을 열어 둔 상태이며 사용자가 로그인한 뒤 작업을 계속해야 한다. **프로젝트 이름과 운영 도메인은 아직 변경되지 않았다.**

## 현재 상태

- **브랜치/원격**: 인계서 갱신 직전 `master`와 `origin/master`가 `beb088f`에서 일치했다.
- **작업 트리**: 인계서 갱신 전 clean. 이 문서만 갱신해 별도 인계 커밋으로 푸시한다.
- **최근 커밋**:
  - `beb088f` Use official transparent Pathway branding
  - `9b248c2` Brand app as a Pathway Partners product
  - `9c812e0` Add handoff for auth layout, company profile editing, signup approval, and admin console work
  - `8f67b0a` Add company reassignment, approve-with-new-org, and unify admin UI
- **배포**: `beb088f`에 대한 GitHub Vercel 상태는 `success`. 공개 워드마크 자산과 투명 파비콘 응답을 직접 검증했다.
- **로컬 개발 서버**: `http://127.0.0.1:3000`에서 실행해 로그인 화면을 검증했다. 다음 세션에서 실행 여부는 다시 확인한다.
- **Supabase 마이그레이션**: `00000000000001`~`00000000000010` 파일 존재. 00007~00010은 원격에 적용됐다. 00004/00005는 원격 객체는 있으나 과거 CLI migration history와 불일치 가능성이 남아 있다.

## 아키텍처 핵심 패턴

- **제품 원칙**: 회사 소개 자료/입력과 최소한의 팀 정보로 Gemini가 NCS 근거를 연결해 JD v1.0을 만든다. 사용자는 선택적 정보를 보완해 v1.1을 만들며, NCS 근거가 검토 루프로 다시 작동해야 한다.
- **인증 라우트 그룹**: `src/app/(app)/` 아래는 인증 필수 영역이다. 공개 인증/초대/콜백 페이지는 그룹 밖에 둔다.
- **활성 조직**: `active_org_id` httpOnly 쿠키와 `getAccessibleOrganizations`/`resolveActiveOrgId`를 재사용한다. 페이지마다 조직 선택 로직을 중복 구현하지 않는다.
- **권한 단일 기준**: `is_org_member`/`is_org_admin` SQL 함수가 RLS와 서버 권한의 기준이다. 함수 조건 변경 시 자기 자신의 멤버십/조직 행이 보이지 않게 되는 부작용을 함께 검사한다.
- **Service Role 격리**: `SUPABASE_SERVICE_ROLE_KEY`는 `src/lib/supabase/admin.ts` 등 서버 전용 코드에서만 사용한다. 클라이언트 컴포넌트에서 import하지 않는다.
- **회사 프로필 버전 관리**: 수정은 `organization_profiles.version_no + 1`의 새 행으로 저장한다. 소비 기능은 원칙적으로 조직의 최신 버전을 조회한다.
- **관리자 UI 토큰**: 관리자 화면은 `src/components/admin/ui.ts`의 공통 스타일 토큰을 사용한다.
- **공식 브랜드 자산**: 워드마크를 HTML 텍스트나 임의 폰트로 재구성하지 않는다. `public/brand/pathway-partners-white.png`, `pathway-partners-color.png`, `pathway-partners-mark.png`을 재사용한다.
- **브랜드 톤**: 어두운 배경은 흰색 워드마크, 밝은 배경은 컬러 워드마크를 사용한다. 파비콘 심볼 배경은 반드시 투명하게 유지한다.
- **검증 후 배포**: `npx tsc --noEmit`, `npm run lint`, `npm run build`를 통과한 변경만 `master`에 푸시한다. Vercel은 GitHub 푸시로 자동 배포된다.

## 주요 파일 경로

- `src/components/auth/auth-shell.tsx` — 인증 화면 좌우 레이아웃 및 브랜드 배치
- `src/components/brand/pathway-brand.tsx` — 공식 워드마크와 저작권 공통 컴포넌트
- `src/components/layout/app-header.tsx` — 인증 후 공통 헤더와 Pathway 제작사 표기
- `src/components/layout/app-footer.tsx` — 인증 후 공통 브랜드 푸터
- `src/app/(app)/layout.tsx` — 인증 필수 공통 레이아웃
- `src/app/layout.tsx` — 전역 메타데이터와 아이콘 엔트리
- `src/app/icon.png`, `src/app/apple-icon.png` — 투명 파비콘/Apple 아이콘
- `public/brand/pathway-partners-white.png` — 어두운 배경용 흰색 전체 워드마크
- `public/brand/pathway-partners-color.png` — 밝은 배경용 컬러 전체 워드마크
- `public/brand/pathway-partners-mark.png` — 투명 컬러 심볼
- `src/lib/org/active-organization.ts` — 활성 회사 해석 공용 로직
- `src/lib/supabase/admin.ts` — Service Role 기반 서버 전용 관리자 클라이언트
- `src/app/(app)/admin/actions.ts` — 회원/가입 승인/소속 변경 Server Actions
- `src/app/(app)/company/actions.ts` — 회사 프로필 새 버전 저장
- `supabase/migrations/00000000000007_organization_access_requests.sql`~`00000000000010_held_org_visibility.sql` — 가입 승인 및 멤버 상태 관련 마이그레이션

## 환경변수

실제 값은 `.env.local` 및 Vercel 환경변수에만 두고 Git/인계서에는 기록하지 않는다.

- `NCS_API_SERVICE_KEY`
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`
- `GEMINI_API_KEY`
- `SENTRY_DSN`, `NEXT_PUBLIC_SENTRY_DSN` — 선택
- `SENTRY_ORG`, `SENTRY_PROJECT`, `SENTRY_AUTH_TOKEN` — 선택

빈 값 템플릿은 `.env.local.example`을 참고한다.

## 미해결 과제

1. **Vercel 프로젝트 및 도메인 변경 — 최우선**
   - 사용자가 Vercel 로그인 화면에서 `isyoon41s-projects`를 관리하는 계정으로 로그인하고 “로그인 완료”라고 알려야 한다.
   - Vercel 프로젝트 이름을 `pathwaypartners-ncsjd-operator`로 변경하고 새 기본 URL을 확인한다.
   - 새 URL 배포 후 `https://pathwaypartners-ncsjd-operator.vercel.app/login`과 `/icon.png`를 검증한다.
   - 기존 URL 유지/리디렉션 여부를 확인한다.
   - Supabase Authentication의 Site URL 및 Redirect URLs에 새 도메인을 추가한다. 전환 중에는 기존 URL을 즉시 제거하지 말고 인증 흐름을 먼저 검증한다.
   - 현재 인계서와 운영 문서의 배포 URL을 새 주소로 갱신한다. 과거 `HANDOFF_20260807.md`는 역사 기록이므로 필요할 때만 주석 처리한다.

2. **관리자 신규 기능 배포 후 사용자 검증**
   - 소속 회사 변경, 승인 시 새 회사 생성, 회원 보류/활성화/삭제를 실제 관리자 계정으로 재확인한다.

3. **마이그레이션 이력 정합성**
   - 00004/00005의 원격 migration history 불일치를 `supabase migration list`로 재확인한다. 원격 객체가 이미 있으므로 무작정 `db push`하지 않는다.

4. **가입 요청 UX 보완**
   - 거절된 사용자의 재신청 상태와 사유 표시, 승인 대기 화면을 개선할 여지가 있다.

5. **제품 고도화 잔여 과제**
   - 문체 가이드/MVC 반영 엔진, NCS 검색 정확도, 버전 이력 UI, 오래된 JD 정리, PDF 자동 테스트, Sentry 활성화, npm audit 취약점 검토가 남아 있다.

## 주의사항

- `.env.local`, Supabase/Gemini/NCS/Sentry 키, `SUPABASE_SERVICE_ROLE_KEY`, DB 비밀번호, Vercel 토큰/브라우저 로그인 정보는 절대 커밋하지 않는다.
- Vercel CLI는 현재 로그아웃 상태다. 토큰을 코드나 셸 기록에 직접 적지 말고, 로그인 세션 또는 안전한 환경변수 방식만 사용한다.
- 도메인을 바꾸기 전에 Supabase 인증 Redirect URL을 함께 계획한다. 새 도메인만 먼저 강제하면 이메일 인증/매직 링크/비밀번호 재설정 콜백이 깨질 수 있다.
- 공식 로고는 제공된 PDF/AI에서 추출한 저장소 자산을 사용한다. CSS/HTML 텍스트로 워드마크를 다시 만들지 않는다.
- 파비콘은 투명 PNG다. 흰색 배경으로 flatten하거나 기존 `favicon.ico`를 복원하지 않는다.
- `src/lib/supabase/admin.ts`를 클라이언트 코드에서 import하지 않는다.
- `deleteUserAccount`는 되돌릴 수 없는 작업이며, `changeUserOrganization`은 현재 단일 소속 모델에 맞춰 기존 멤버십을 삭제한다.
- 작업 트리에 다른 사용자의 변경이 있으면 덮어쓰지 말고 diff로 범위를 확인한다.
